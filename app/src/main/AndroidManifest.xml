<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swufe.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyListActivity"></activity>
        <activity android:name=".RateCalc" android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity android:name=".Mylist2Activity" />
        <activity android:name=".RateListActivity" />
        <activity android:name=".Config" />
        <activity android:name=".RateChange" />
        <activity android:name=".RecordScore" />
        <activity
            android:name=".TempTran"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>